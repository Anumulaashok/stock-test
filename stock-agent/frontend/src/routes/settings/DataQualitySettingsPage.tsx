import { ComingSoonPage } from '../ComingSoonPage'

/** Placeholder -- built out by Phase 2 Agent E. No global data-quality
 * endpoint exists; this must be per-ticker (a ticker picker running
 * `DataQualitySection` for that ticker's latest report), never a
 * synthesized global score. */
export function DataQualitySettingsPage() {
  return <ComingSoonPage title="Data Quality" />
}
