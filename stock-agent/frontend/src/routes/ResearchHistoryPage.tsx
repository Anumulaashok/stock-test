import { ComingSoonPage } from './ComingSoonPage'

/** Placeholder -- built out by Phase 2 Agent B, backed by Phase 3
 * Agent H's new `GET /api/v1/research/recent` (cross-ticker listing --
 * no such endpoint exists yet). Public, global (research has no
 * `user_id`), never "your" research. */
export function ResearchHistoryPage() {
  return <ComingSoonPage title="Research" />
}
