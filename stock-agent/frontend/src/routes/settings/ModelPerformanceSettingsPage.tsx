import { ComingSoonPage } from '../ComingSoonPage'

/** Placeholder -- built out by Phase 2 Agent E, backed by the real (but
 * currently unused) `fetchForecastAccuracy` and Phase 3 Agent I's
 * score-outcome recording. Per-ticker only -- no cross-ticker accuracy
 * endpoint exists; score-bucket performance renders as "insufficient
 * backtest history" until real history accrues, never a fabricated
 * number. */
export function ModelPerformanceSettingsPage() {
  return <ComingSoonPage title="Model Performance" />
}
