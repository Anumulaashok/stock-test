"""Portfolio/watchlist domain: `PortfolioService` (`service.py`) owns all
holding/watchlist CRUD and the market-value/unrealized-gain calculations,
with every operation scoped to the requesting user.
"""
