import { ComingSoonPage } from './ComingSoonPage'

/** Placeholder -- built out by Phase 2 Agent D (Watchlist + Portfolio),
 * extracted from `DashboardPage.tsx`'s inlined `HoldingsTable`. */
export function PortfolioPage() {
  return <ComingSoonPage title="Portfolio" />
}
