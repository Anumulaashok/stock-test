"""User authentication: password hashing + JWT bearer tokens
(`security.py`), signup/login/logout/current-user business logic
(`service.py`), and the FastAPI dependency that resolves the
authenticated user from an `Authorization` header (`dependencies.py`).
"""
